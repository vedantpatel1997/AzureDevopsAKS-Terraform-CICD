trigger: none
pr: none

pool:
  vmImage: 'ubuntu-latest'

parameters:
- name: targetEnvironments
  displayName: Target environments to destroy
  type: stringList
  default:
  - dev
  values:
  - dev
  - qa
  - prod

variables:
- name: TERRAFORM_MANIFESTS_SOURCE_DIR
  value: '$(System.DefaultWorkingDirectory)/AKS-Terraform/Terraform-manifests'
- name: TERRAFORM_MANIFESTS_WORK_DIR
  value: '$(Pipeline.Workspace)/Terraform-manifests-out'
- name: TERRAFORM_SERVICE_CONNECTION
  value: 'terraform-aks-azurerm-svc-con'
- name: TERRAFORM_STATE_RESOURCE_GROUP
  value: 'TerraformStorageAccount'
- name: TERRAFORM_STATE_STORAGE_ACCOUNT
  value: 'strgterraformvp'
- name: TERRAFORM_STATE_CONTAINER
  value: 'aks-tfstate'
- name: SSH_PUBLIC_KEY_FILE
  value: '$(Pipeline.Workspace)/secure-files/aks-terraform-devops-ssh-key-ubuntu.pub'

stages:
- stage: TerraformValidate
  displayName: Validate Terraform for Destroy
  jobs:
  - job: TerraformValidateJob
    displayName: Validate Terraform Manifests
    continueOnError: false
    steps:
    - task: PublishPipelineArtifact@1
      displayName: Publish Terraform Manifests
      inputs:
        targetPath: '$(TERRAFORM_MANIFESTS_SOURCE_DIR)'
        artifact: 'Terraform-manifests-out'
        publishLocation: 'pipeline'

    - task: TerraformInstaller@1
      displayName: Install Terraform
      inputs:
        terraformVersion: 'latest'

    - task: TerraformTask@5
      displayName: Terraform Init
      inputs:
        provider: 'azurerm'
        command: 'init'
        workingDirectory: '$(TERRAFORM_MANIFESTS_SOURCE_DIR)'
        backendServiceArm: '$(TERRAFORM_SERVICE_CONNECTION)'
        backendAzureRmResourceGroupName: '$(TERRAFORM_STATE_RESOURCE_GROUP)'
        backendAzureRmStorageAccountName: '$(TERRAFORM_STATE_STORAGE_ACCOUNT)'
        backendAzureRmContainerName: '$(TERRAFORM_STATE_CONTAINER)'
        backendAzureRmKey: 'aks-dev.tfstate'

    - task: TerraformTask@5
      displayName: Terraform Validate
      inputs:
        provider: 'azurerm'
        command: 'validate'
        workingDirectory: '$(TERRAFORM_MANIFESTS_SOURCE_DIR)'

- ${{ each environmentName in parameters.targetEnvironments }}:
  - stage: TerraformDestroyPlan_${{ environmentName }}
    displayName: Create Destroy Plan (${{ environmentName }})
    dependsOn: TerraformValidate
    jobs:
    - job: TerraformDestroyPlanJob_${{ environmentName }}
      displayName: Generate Terraform Destroy Plan (${{ environmentName }})
      steps:
      - task: DownloadPipelineArtifact@2
        displayName: Download Terraform Manifests
        inputs:
          buildType: 'current'
          artifactName: 'Terraform-manifests-out'
          targetPath: '$(TERRAFORM_MANIFESTS_WORK_DIR)'

      - task: DownloadSecureFile@1
        displayName: Download SSH Public Key
        name: sshkey
        inputs:
          secureFile: 'aks-terraform-devops-ssh-key-ubuntu.pub'

      - script: |
          mkdir -p "$(Pipeline.Workspace)/secure-files"
          cp "$(sshkey.secureFilePath)" "$(SSH_PUBLIC_KEY_FILE)"
        displayName: Stage SSH Public Key

      - task: TerraformInstaller@1
        displayName: Install Terraform
        inputs:
          terraformVersion: 'latest'

      - task: TerraformTask@5
        displayName: Terraform Init
        inputs:
          provider: 'azurerm'
          command: 'init'
          workingDirectory: '$(TERRAFORM_MANIFESTS_WORK_DIR)'
          backendServiceArm: '$(TERRAFORM_SERVICE_CONNECTION)'
          backendAzureRmResourceGroupName: '$(TERRAFORM_STATE_RESOURCE_GROUP)'
          backendAzureRmStorageAccountName: '$(TERRAFORM_STATE_STORAGE_ACCOUNT)'
          backendAzureRmContainerName: '$(TERRAFORM_STATE_CONTAINER)'
          backendAzureRmKey: 'aks-${{ environmentName }}.tfstate'

      - task: TerraformTask@5
        displayName: Terraform Plan Destroy
        inputs:
          provider: 'azurerm'
          command: 'plan'
          workingDirectory: '$(TERRAFORM_MANIFESTS_WORK_DIR)'
          commandOptions: '-destroy -var ssh_public_key=$(SSH_PUBLIC_KEY_FILE) -var environment=${{ environmentName }} -out $(TERRAFORM_MANIFESTS_WORK_DIR)/${{ environmentName }}-destroy-$(Build.BuildId).tfplan'
          environmentServiceNameAzureRM: '$(TERRAFORM_SERVICE_CONNECTION)'

      - script: |
          mkdir -p "$(Pipeline.Workspace)/destroy-plan-bundle-${{ environmentName }}"
          terraform show -no-color "$(TERRAFORM_MANIFESTS_WORK_DIR)/${{ environmentName }}-destroy-$(Build.BuildId).tfplan" > "$(Pipeline.Workspace)/destroy-plan-bundle-${{ environmentName }}/${{ environmentName }}-destroy-$(Build.BuildId).txt"
          cp "$(TERRAFORM_MANIFESTS_WORK_DIR)/${{ environmentName }}-destroy-$(Build.BuildId).tfplan" "$(Pipeline.Workspace)/destroy-plan-bundle-${{ environmentName }}/${{ environmentName }}-destroy-$(Build.BuildId).tfplan"
        displayName: Prepare Destroy Plan Bundle
        workingDirectory: '$(TERRAFORM_MANIFESTS_WORK_DIR)'

      - task: PublishPipelineArtifact@1
        displayName: Publish Destroy Plan Bundle
        inputs:
          targetPath: '$(Pipeline.Workspace)/destroy-plan-bundle-${{ environmentName }}'
          artifact: '${{ environmentName }}-destroy-plan'
          publishLocation: 'pipeline'

  - stage: TerraformDestroy_${{ environmentName }}
    displayName: Destroy AKS Cluster (${{ environmentName }})
    dependsOn: TerraformDestroyPlan_${{ environmentName }}
    jobs:
    - deployment: DestroyAKSCluster_${{ environmentName }}
      displayName: Destroy ${{ environmentName }} AKS Cluster
      environment: '${{ environmentName }}'
      strategy:
        runOnce:
          deploy:
            steps:
            - task: DownloadPipelineArtifact@2
              displayName: Download Terraform Manifests
              inputs:
                buildType: 'current'
                artifactName: 'Terraform-manifests-out'
                targetPath: '$(TERRAFORM_MANIFESTS_WORK_DIR)'

            - task: DownloadPipelineArtifact@2
              displayName: Download Reviewed Destroy Plan
              inputs:
                buildType: 'current'
                artifactName: '${{ environmentName }}-destroy-plan'
                targetPath: '$(Pipeline.Workspace)/destroy-plan-bundle-${{ environmentName }}'

            - task: DownloadSecureFile@1
              displayName: Download SSH Public Key
              name: sshkey
              inputs:
                secureFile: 'aks-terraform-devops-ssh-key-ubuntu.pub'

            - script: |
                mkdir -p "$(Pipeline.Workspace)/secure-files"
                cp "$(sshkey.secureFilePath)" "$(SSH_PUBLIC_KEY_FILE)"
              displayName: Stage SSH Public Key

            - task: TerraformInstaller@1
              displayName: Install Terraform
              inputs:
                terraformVersion: 'latest'

            - task: TerraformTask@5
              displayName: Terraform Init
              inputs:
                provider: 'azurerm'
                command: 'init'
                workingDirectory: '$(TERRAFORM_MANIFESTS_WORK_DIR)'
                backendServiceArm: '$(TERRAFORM_SERVICE_CONNECTION)'
                backendAzureRmResourceGroupName: '$(TERRAFORM_STATE_RESOURCE_GROUP)'
                backendAzureRmStorageAccountName: '$(TERRAFORM_STATE_STORAGE_ACCOUNT)'
                backendAzureRmContainerName: '$(TERRAFORM_STATE_CONTAINER)'
                backendAzureRmKey: 'aks-${{ environmentName }}.tfstate'

            - task: TerraformTask@5
              displayName: Terraform Apply Destroy Plan
              inputs:
                provider: 'azurerm'
                command: 'apply'
                workingDirectory: '$(TERRAFORM_MANIFESTS_WORK_DIR)'
                commandOptions: '$(Pipeline.Workspace)/destroy-plan-bundle-${{ environmentName }}/${{ environmentName }}-destroy-$(Build.BuildId).tfplan'
                environmentServiceNameAzureRM: '$(TERRAFORM_SERVICE_CONNECTION)'
